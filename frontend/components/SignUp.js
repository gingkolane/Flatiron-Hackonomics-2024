import { StyleSheet, Text, View } from "react-native";
import React from "react";

const SignUp = ({ navigation, route }) => {
  return <Text>Sign Up here!</Text>;
};

export default SignUp;

const styles = StyleSheet.create({});
