import { StyleSheet, Text, View } from "react-native";
import React from "react";

const SignIn = ({ navigation, route }) => {
  return <Text className="bg-black">Sign in here</Text>;
};

export default SignIn;

const styles = StyleSheet.create({});
